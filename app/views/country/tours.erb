<h2 style="text-align: center;"><%= @country.country_name %></h2>

<% @tours.each do |tour| %>
	<a href="/tour/<%= tour.id %>" id="tourlink"><%= tour.tour_name %></a>
<% end %><br>
<% @tours.each do |tour| %>
		<a href="/tour/<%= tour.id %>"><img src="<%= tour.images.first.url %>" id="tourimages" height="300" width="356"></a>
<% end %>
